# Default environment settings for the local runner

# This is needed to override the URL that's used for the redirection after
# checkout which is hardcoded in @wordpress/e2e-test-utils/build-module/shared/config.js
# on the newer versions of that package (^8.0.0)
WP_BASE_URL='http://localhost:8084'

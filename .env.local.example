# Copy this file to .env.local and customize as needed.
# Values are auto-populated on first `npm run up` if not set.

# Port for this worktree's WordPress instance (default: auto-detected)
# WP_PORT=8086

# Unique identifier for this worktree (default: directory basename)
# WORKTREE_ID=feature-abc

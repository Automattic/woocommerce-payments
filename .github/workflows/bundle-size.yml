name: Compressed Size

on: [pull_request]

jobs:
    build:
        name: Bundle size check
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2
            - uses: preactjs/compressed-size-action@v2
              with:
                  pattern: '{release/**/*.js,release/**/*.tsx,release/**/*.css}'

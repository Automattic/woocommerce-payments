/** @format */
/**
 * External dependencies
 */
import React from 'react';

const EnabledCurrenciesList = ( { className, children } ) => {
	return <ul className={ className }>{ children }</ul>;
};

export default EnabledCurrenciesList;

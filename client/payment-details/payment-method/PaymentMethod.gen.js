/* Untyped file generated from PaymentMethod.re by genType. */
/* eslint-disable */

import {__ as __NotChecked} from '@wordpress/i18n';

import {sprintf as sprintfNotChecked} from '@wordpress/i18n';

// In case of type error, check the type of '__' in 'PaymentMethod.re' and '@wordpress/i18n'.
export const __TypeChecked = __NotChecked;

// Export '__' early to allow circular import from the '.bs.js' file.
export const __ = __TypeChecked;

// In case of type error, check the type of 'sprintf' in 'PaymentMethod.re' and '@wordpress/i18n'.
export const sprintfTypeChecked = sprintfNotChecked;

// Export 'sprintf' early to allow circular import from the '.bs.js' file.
export const sprintf = sprintfTypeChecked;

const PaymentMethodBS = require('./PaymentMethod.bs');

export const formatPaymentMethodDetails = PaymentMethodBS.formatPaymentMethodDetails;

/* Untyped file generated from selectors.re by genType. */
/* eslint-disable */

import * as Curry from 'bs-platform/lib/es6/curry.js';

import * as selectorsBS from './selectors.bs';

export const getCharge = function (Arg1, Arg2) {
  const result = Curry._2(selectorsBS.getCharge, Arg1, Arg2);
  return result
};

export const getChargeError = function (Arg1, Arg2) {
  const result = Curry._2(selectorsBS.getChargeError, Arg1, Arg2);
  return result
};

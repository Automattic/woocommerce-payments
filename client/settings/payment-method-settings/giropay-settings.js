/** @format */
/**
 * External dependencies
 */
import { Card, CardBody } from '@wordpress/components';

const GiropaySettings = () => {
	return (
		<Card>
			<CardBody>Giropay settings placeholder.</CardBody>
		</Card>
	);
};

export default GiropaySettings;

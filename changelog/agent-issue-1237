Significance: patch
Type: fix

Ensure the `wcpay_checkout_has_recurring_items` filter is consulted when deciding whether a payment is recurring even if the WooCommerce Subscriptions plugin is not active, so extensions can opt an order into saving the payment method without requiring Subscriptions.

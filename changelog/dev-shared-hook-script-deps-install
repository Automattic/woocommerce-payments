Significance: patch
Type: dev
Comment: Add shared .husky/install-deps.sh script that runs PHP deps (composer install) first, then shows a prominent yellow banner when pnpm-lock.yaml has changed, replacing the easy-to-miss plain-text warning from the pnpm migration.

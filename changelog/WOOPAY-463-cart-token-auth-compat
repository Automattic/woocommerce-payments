Significance: minor
Type: update

Add opt-in support for authorizing proxied WooPay Store API requests on the Cart-Token the store already validates, and accept an encrypted attested-email envelope on the WooPay session route, so neither channel needs a Jetpack blog token signature
